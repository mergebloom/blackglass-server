{
  "name": "blackglass-server",
  "version": "0.4.1",
  "private": true,
  "license": "MIT",
  "engines": {
    "bun": ">=1.3.0"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:linux": "./ops/build-all-linux-releases.sh",
    "build:linux:amd64": "./ops/build-linux-release.sh linux-amd64",
    "build:linux:arm64": "./ops/build-linux-release.sh linux-arm64",
    "build:release": "./ops/build-release.sh",
    "licenses:check": "bun run tools/third-party-notices.ts",
    "licenses:generate": "bun run tools/third-party-notices.ts --write",
    "server": "cargo run --release --manifest-path apps/server-rust/Cargo.toml -- serve",
    "server:bun-oracle": "bun run apps/server/src/index.ts",
    "server:check": "cargo check --manifest-path apps/server-rust/Cargo.toml",
    "server:test": "cargo test --locked --manifest-path apps/server-rust/Cargo.toml",
    "server:measure": "bun run tools/measure-rust-server.ts",
    "test:integration": "bun test tests/server.integration.test.ts",
    "verify:release-metadata": "./ops/verify-release-metadata.sh",
    "check:fast": "bun run verify:release-metadata && bun run licenses:check && bun run typecheck && cargo fmt --manifest-path apps/server-rust/Cargo.toml -- --check && cargo check --locked --manifest-path apps/server-rust/Cargo.toml --all-targets && bun test tests/release-metadata.test.ts tests/client-contract.integration.test.ts tests/collaboration.integration.test.ts tests/tenant-isolation.integration.test.ts",
    "check:full": "bun run verify:release-metadata && bun run licenses:check && bun run typecheck && cargo fmt --manifest-path apps/server-rust/Cargo.toml -- --check && cargo clippy --locked --manifest-path apps/server-rust/Cargo.toml --all-targets -- -D warnings && bun run server:test && bun test",
    "check:release": "bun run check:full && shellcheck ops/*.sh",
    "check": "bun run check:full",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.2.22",
    "typescript": "^5.9.2"
  }
}
